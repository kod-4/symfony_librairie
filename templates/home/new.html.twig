{% extends 'base.html.twig' %}

{% block title %}New Home{% endblock %}

{% block body %}
<div class="col-8 mx-auto">
    <h1 class="text-center">Create new Home</h1>
    <a class="btn btn-outline-primary" href="{{ path('home_index') }}">back to list</a>
    {{ include('home/_form.html.twig') }}
</div>
{% endblock %}
